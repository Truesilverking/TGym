# Exercise media — fetched on first `docker compose up` (or via scripts/fetch-media.sh)
media/img/
media/gif/

# Runtime data — users, passkeys, per-user state, session secret
data/

# Local config
.env

# Build output / deps
frontend/dist/
frontend/.tools/
.tools/
frontend/package-staging-*/
frontend/docs/REQUEST_*.md
node_modules/
*.log
.DS_Store
node_modules
.claude/
.agents/

# Generated real PCM fixtures for Android audio instrumentation
frontend/android/app/src/androidTest/assets/audio-audit.json
